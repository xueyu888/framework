1. 现在整体思路已经比较明确了：
    1） 先将文档按段落拆分为段落块；
    2） 然后对每个段落块进行角色识别，识别为标题或正文；
    3） 根据1标题+n正文的方式，将段落块打包为文本块；
    4） 完成所有打包后将文本块组合成为最终输出文档。

但是考虑一个模块，还要考虑实际落地。
从大往小进行考虑：
1. L0模块级别：模块输入的内容与格式。
需要考虑该模块前后级的兼容能力。要考虑以下情况：
1）如果一个文本更新后。
前级：
<输入>输入为单个md模式的纯文本，已经进行了换行、空白、编码归一化处理。
<输出>输出为一个已经完成分块的完整文档。包括：原文档ID，n*[文本块编号，文本块内容]
      可选内容：段落块索引信息表：记录了每个段落块的开始/结束位置，角色，归属哪个文本块。此外还包含原文档ID。


