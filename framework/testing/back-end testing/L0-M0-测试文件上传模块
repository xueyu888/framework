# 测试文件上传模块:TestFileUpload

@framework

## 1. 能力声明（Capability Statement）

- `C1` 测试文件上传能力：测试将用户本地的文件数据发送并保存到服务器上的能力。
- `C2` 测试查询所有文件列表能力：测试获取服务器上已经上传的文件列表的能力。
- `C3` 测试查询单个特定文件能力：测试系统会根据特定文件的哈希值能返回单个特定文件的详细属性或内容的能力。
- `C4` 测试删除文件能力：测试系统会根据传入指定的 ID 把对应的文件从服务器中彻底删除的能力。

### 非职责声明（Non-Responsibility Statement）

- `N1` 非职责声明：不负责文件内容的深度业务解析与校验。

## 2. 边界定义（Boundary / 参数）

- `POSTFILE` 边界：file (属性: 必选，类型: string($binary), 有效范围: 格式txt|csv 编码utf-8) 
                  table_name (属性: 可选，类型: string | (string | null))
                  device_name (属性: 必选，类型: string，有效范围: 已存在的device_name)
                  skip（属性: 可选，类型: integer, 有效范围:[`0`,`上传文件总行数-1`]）来源：`C1`。
- `GETFILE` 边界：page (属性: 可选，类型: integer，默认值: 1，有效范围:≥1)
                  limit (属性: 可选，类型: integer, 默认值: 20，有效范围:≥1)
                  table_name (属性: 可选，类型: string | (string | null))
                  user（属性: 可选，类型: string | (string | null)）
                  device_name（属性: 可选，类型: string | (string | null)）来源：`C2`。
- `GETSINGLEFILE` 边界：hash_table_name (属性: 必选，类型: string, 有效范围: 已存在的hash_table_name) 来源：`C3`。
- `DELETEFILE` 边界：hash_table_name (属性: 必选，类型: string, 有效范围: 已存在的hash_table_name) 来源：`C4`。

## 3. 最小可行基（Minimum Viable Bases）
    
- `B1` 测试文件上传基：
       由
         调用接口: POST /api/file
         输入参数：file
                  table_name 
                  device_name
                  skip
         调用结果：调用成功状态码返回2000，调用失败状态码返回!2000
       构成的测试文件上传结构。来源：`C1 + POSTFILE`。

- `B2` 测试查询所有文件列表基：
       由
         调用接口: GET /api/file
         输入参数：page
                  limit 
                  table_name 
                  user
                  device_name
         调用结果：调用成功状态码返回2000，调用失败状态码返回!2000
       构成的测试查询所有文件列表结构。来源：`C2 + GETFILE`。

- `B3` 测试查询单个特定文件基：
       由
         调用接口: GET /api/single/file/{hash_table_name}
         输入参数：hash_table_name
         调用结果：调用成功状态码返回2000，调用失败状态码返回!2000
       构成的测试查询单个特定文件结构。来源：`C3 + GETSINGLEFILE`。


- `B4` 测试删除文件基：
       由
         调用接口: DELETE /api/file/{id}
         输入参数：id
         调用结果：调用成功状态码返回2000，调用失败状态码返回!2000
       构成的测试删除文件结构。来源：`C4 + DELETEFILE`。


## 4. 基组合原则（Base Combination Principles）

- `R1` 独立调用原则
  - `R1.1` 参与基：`B1`、`B2`。
  - `R1.2` 组合方式：`B1` 作为创建测试资源的起点可独立调用，`B2` 作为无副作用列表查询可独立调用。
  - `R1.3` 输出能力：`C1`、`C2`。
  - `R1.4` 边界绑定：`POSTFILE + GETFILE`。

- `R2` 依赖调用原则
  - `R2.1` 参与基：`B1 + B3`、`B1 + B4`。
  - `R2.2` 组合方式：`B3`、`B4` 不可单独调用，必须先由 `B1` 成功创建测试文件，再分别使用该轮 `B1` 产出的 `hash_table_name` 与 `id` 调用。
  - `R2.3` 输出能力：`C1 + C3`、`C1 + C4`。
  - `R2.4` 边界绑定：`POSTFILE + GETSINGLEFILE + DELETEFILE`。

- `R3` 查询增强原则
  - `R3.1` 参与基：`B1 + B2`、`B1 + B2 + B3`。
  - `R3.2` 组合方式：先上传，再查询列表，必要时继续查询单文件详情，用于增强对上传结果可见性与可追踪性的覆盖。
  - `R3.3` 输出能力：`C1 + C2`、`C1 + C2 + C3`。
  - `R3.4` 边界绑定：`POSTFILE + GETFILE + GETSINGLEFILE`。

- `R4` 生命周期闭环原则
  - `R4.1` 参与基：`B1 + B2 + B4`、`B1 + B3 + B4`、`B1 + B2 + B3 + B4`。
  - `R4.2` 组合方式：凡包含 `B4` 的组合，必须先完成本轮所需查询断言，再执行删除；`B4` 为该轮最后一个基，删除后本轮结束。
  - `R4.3` 输出能力：`C1 + C2 + C4`、`C1 + C3 + C4`、`C1 + C2 + C3 + C4`。
  - `R4.4` 边界绑定：`POSTFILE + GETFILE + GETSINGLEFILE + DELETEFILE`。

- `R5` 随机有效组合原则
  - `R5.1` 参与基：`{B1, B2, B3, B4}` 的随机子集。
  - `R5.2` 有效组合：`B1`、`B2`、`B1 + B2`、`B1 + B3`、`B1 + B4`、`B1 + B2 + B3`、`B1 + B2 + B4`、`B1 + B3 + B4`、`B1 + B2 + B3 + B4`。
  - `R5.3` 排序规则：若组合中包含 `B3` 或 `B4`，则 `B1` 必须在前；若组合中包含 `B4`，则 `B4` 必须在最后；`B2` 与 `B3` 若同时存在，可在 `B1` 之后、`B4` 之前任意排序。
  - `R5.4` 失效组合：`B3` 单独调用、`B4` 单独调用、`B2 + B3`、`B2 + B4`、`B3 + B4` 及任何违反 `R5.3` 的顺序组合，均视为无效组合，不进入执行。

- `R6` 参数一致性原则
  - `R6.1` 参与基：所有多基组合。
  - `R6.2` 组合方式：同一轮组合中，`B2`、`B3`、`B4` 的查询条件、断言对象与删除对象必须全部来自同一轮 `B1` 的返回结果，不允许引用历史残留数据。
  - `R6.3` 输出能力：保证组合可追踪、可复现、可重复执行。
  - `R6.4` 边界绑定：`POSTFILE + GETFILE + GETSINGLEFILE + DELETEFILE`。

 
## 5. 验证（Verification）

- `V1` 基本调用成功性：`B1`、`B2` 独立调用时必须返回 `2000`；`B1` 的成功响应中必须能提取到后续组合所需的 `id` 与 `hash_table_name`。

- `V2` 依赖调用成功性：凡执行 `B3`、`B4` 的组合，必须先完成该轮 `B1`，并分别使用该轮 `B1` 产出的 `hash_table_name`、`id` 调用；调用结果必须返回 `2000`。

- `V3` 组合生成正确性：随机生成组合时，只允许命中 `R5.2` 中定义的 9 类有效组合；命中无效组合时必须在执行前剔除并重新生成。

- `V4` 调用顺序正确性：所有执行中的组合必须满足 `R5.3` 的顺序规则；若顺序不满足，则该轮直接判失败，不得继续调用后续基。

- `V5` 参数传递一致性：同一轮组合中的 `B2`、`B3`、`B4` 必须围绕同一测试文件实体完成断言，确保列表、详情、删除操作针对的是同一个上传结果。

- `V6` 查询覆盖有效性：凡执行包含 `B2` 或 `B3` 的组合，必须验证目标文件可在列表或单文件详情中被准确检出；凡同时包含 `B2` 与 `B3`，则两者结果必须一致指向同一文件。

- `V7` 删除闭环有效性：凡执行包含 `B4` 的组合，删除前必须已完成本轮全部查询断言；删除成功后本轮结束，后续若继续测试必须重新由 `B1` 创建新文件。

- `V8` 组合覆盖充分性：测试执行至少应覆盖 `R5.2` 中全部 9 类有效组合各 1 次；若采用随机抽样，则必须记录已覆盖组合类别，直到 9 类全部命中。

- `V9` 重复执行稳定性：多轮随机执行时，所有有效组合调用成功率必须为 `100%`；每轮需使用独立测试数据，避免环境残留影响下一轮结果。

- `V10` 结论表达：每轮执行后必须输出组合类别、调用顺序、关键参数传递链、各基状态码与最终通过/失败结论。
