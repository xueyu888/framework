# 文件块存取模块:FileChunkStorageModule

@framework

## 1. 能力声明（Capability Statement）

- `C1` 存取单块/批量块能力: 支持以单块或受限批量为单位，保存和读取单个或批量文件块的信息
- `C2` 文件块与文件对应能力: 能查询某个文件拥有的文件块, 或根据某个文件块查询其所属的文件
- `C3` 集合销毁能力: 可根据文件标识，一次性删除一个文件拥有的所有文件块

### 非职责声明（Non-Responsibility Statement）

- `N1` 不提供更新能力: 不提供对单独某个文件块的更新能力
- `N2` 不提供单点销毁能力: 不提供针对单一文件块的独立删除能力

## 2. 边界定义（Boundary / 参数）

- `P1` 文件块大小边界: 每个文件块的大小限制, 对应所选的存储载体. 来源: `C1`
- `P2` 文件块存取载体边界: 关系型数据库(VARCHAR, TEXT, MEDIUMTEXT), minio, 内存
- `P3` 文件块批量存放数量边界: 每批次存放的文件块数量存在限制. 来源: `C1`
- `P4` 文件块批量取出数量边界: 每批次取出的文件块数量存在限制. 来源: `C1`
- `P5` 获取文件中的块索引数量边界: 每次获取一个文件中拥有的文件块的索引的数量存在限制. 来源: `C2`
- `P6` 销毁粒度边界: 执行销毁操作时，入参必须为文件标识，不可为单一分块标识. 来源: `C3`

## 3. 最小可行基（Minimum Viable Bases）

- `B1` 块存取结构. 来源: `C1 + P1 + P2`
- `B2` 1 对 N 映射结构. 来源: `C2 + P3 + P4 + P5`
- `B3` 
- `B4` 

## 4. 基组合原则（Base Combination Principles）

- `R1` 结构装配组合

## 5. 验证（Verification）

- `V1` 推导一致性：每个 `B*` 必须能由至少一个 `C*` 与一个参数项推导得到。
- `V2` 规则一致性：每个 `R*` 必须明确参与基/组合方式/输出能力或失效结论/边界绑定。
- `V3` 结构闭环性：`R1~R2` 输出能力并集必须覆盖 `C1~C3`。
- `V4` 边界符合性：所有有效组合必须满足绑定的参数边界。
- `V5` 最小必要性：移除任一 `B*` 后，`V3` 或 `V4` 至少一项失败。
- `V6` 结论表达：逐条输出 `R* -> C* + 边界绑定` 或 `R* -> N* + 边界绑定` 的通过或失败结论。