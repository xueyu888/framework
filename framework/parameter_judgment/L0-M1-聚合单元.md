# 聚合单元:AggUnit

@framework

## 0. 目标 (Goal)

将一段时序数据压缩为单个特征值，供后续规则判断使用


## 1. 最小可行基（Minimum Viable Bases）

- `B1` 聚合函数：例如最大值、最小值等，用于对队列中的数据进行聚合


## 2. 基组合原则（Base Combination Principles）


## 3. 能力声明（Capability Statement）

### 非职责声明（Non-Responsibility Statement）


## 4. 边界定义（Boundary / 参数）

### 3.1 接口定义（IO / Ports）

- `P1` 输入约束：必须是浮点数数组

### 3.2 参数边界（Parameter Constraints）

- `P1` 浮点数值域：float64
- `P2` 数组最大长度：uint64
- `P3` 聚合函数类型范围：求和、最大值、最小值、平均值、方差、斜率


## 5. 验证（Verification）
