## 中文说明

- 同步当前 Shelf AI 的治理口径：发布说明与打包文档现在明确把“主链路”定义为 `Framework -> Product Spec -> Implementation Config -> Code -> Evidence` 的收敛路径，以及代码/证据反查回上游结构的校验路径。
- 明确当前覆盖边界，避免把治理能力说得过满：结构主链路中的项目发现、编译产物、运行时装配、证据物化和反查 compare 属于强覆盖；更底层的 renderer/style/script 细节仍可能只处于部分治理状态。
- 这次发布是一次口径与发布产物同步更新，让公开 GitHub Release、插件内 README 和仓库中的第二轮审计结论保持一致。

## English Notes

- Sync the current Shelf AI governance posture: the packaged docs and release notes now define the "mainline" explicitly as the `Framework -> Product Spec -> Implementation Config -> Code -> Evidence` convergence path plus the reverse validation path from code/evidence back to upstream structure.
- Clarify the real coverage boundary instead of overstating closure: project discovery, compiled specs/contracts, runtime assembly, evidence materialization, and reverse governance compare are strongly covered on the structural mainline, while lower-level renderer/style/script details can still remain only partially governed.
- This release is a posture-and-artifact sync so the public GitHub Release, the packaged README, and the repository-side second-round audit all describe the same current governance state.
