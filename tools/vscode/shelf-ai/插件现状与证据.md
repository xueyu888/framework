# Shelf AI 插件现状与证据

## 0. 文档定位

本文档只记录“当前实现现状”和“可核验证据”，不定义契约约束。  
契约约束统一以 `tools/vscode/shelf-ai/插件设计与实现契约.md` 为准。

快照日期：`2026-04-09`

## 1. 对照方式

每条记录按以下结构维护：

- 契约条目：对应契约文档中的约束项
- 当前现状：仓库当前代码/配置体现出的状态
- 证据：可直接定位的文件或验证命令

## 2. 配置契约相关现状（对应契约第 6 章）

| 契约条目 | 当前现状 | 证据 |
| --- | --- | --- |
| settings-first 覆盖插件运行时调优项 | 运行时行为与命令入口通过 VSCode setting 暴露，插件侧读取并应用 | `tools/vscode/shelf-ai/package.json`（`contributes.configuration`）、`tools/vscode/shelf-ai/extension.js` |
| 右下角通知弹窗统一开关 | 支持 `shelf.showMessagePopups` 全局控制通知弹窗；关闭后保留 output/status/problems 信号，不吞掉失败信息 | `tools/vscode/shelf-ai/package.json`、`tools/vscode/shelf-ai/extension.js`、`tools/vscode/shelf-ai/README.md` |
| framework 文档实时语法诊断 | `framework/**`、`framework_drafts/**` 下的 Markdown，以及正文显式带 `@framework` 的任意 Markdown，都会触发独立于 project 选择的实时语法诊断（Problems 波浪线） | `tools/vscode/shelf-ai/framework_lint.js`、`tools/vscode/shelf-ai/extension.js`、`tools/vscode/shelf-ai/package.json` |
| framework 附件闭包约束 | `framework/**` 与 `framework_drafts/**` 下的非 module Markdown 只有被同域 module 直接引用时才有效；孤立 Markdown、跨域 Markdown 引用、目录外 Markdown 旁路都会报错 | `tools/vscode/shelf-ai/framework_lint.js`、`tools/vscode/shelf-ai/test_framework_lint.js` |
| framework 文档上下文补全与 Quick Fix | 补全支持章节/符号上下文与默认编号推断；lint 诊断支持 lightbulb 快速修复 | `tools/vscode/shelf-ai/framework_completion.js`、`tools/vscode/shelf-ai/extension.js`、`tools/vscode/shelf-ai/test_snippets.js` |
| `.sf` 实验语言预览 | 独立 `shelf-framework` language id 已切到关键字优先的 authoring 预览：顶层块统一为单数 `Goal / Base / Principle / Space / Boundary`，其下的 `set / elem / struct / seq / map / op[...]`、`sat / eq / map`、`set / comb / seq`、`in / out / param<enum|range>` 等声明共享同一套 grammar 驱动的语义高亮、TextMate 兜底高亮、实时 lint 与上下文补全；打开 `.sf` 时插件会直接激活，并对该语言默认开启 semantic highlighting；声明头保持单行，但声明名、`[shape]` 与 `<subtype>` 片段会分别高亮；右值允许按更深缩进续写；`map` 用于显式映射/索引/传播关系，`op[...]` 用于带 shape 的操作子；引用前缀同步收敛为 `Base.xxx / Principle.xxx / Space.xxx / Boundary.xxx`；`Boundary` 只承载输入/输出/参数，不再接受 `op[...]` 形式；`in/out/param` 的 subtype 采用开放语义标签，右值既可写字面集合，也可直接写 `Base.xxx` 引用；当前团队约定 `{}` 表示集合或枚举内容，`<>` 表示有序集合；该语言只用于编辑器预览，不进入 canonical / publish 主链 | `tools/vscode/shelf-ai/sf_grammar.js`、`tools/vscode/shelf-ai/sf_textmate_grammar.js`、`tools/vscode/shelf-ai/sf_completion.js`、`tools/vscode/shelf-ai/sf_lint.js`、`tools/vscode/shelf-ai/languages/shelf-framework.tmLanguage.json`、`tools/vscode/shelf-ai/extension.js`、`tools/vscode/shelf-ai/package.json`、`tools/vscode/shelf-ai/test_sf_language.js`、`tools/vscode/shelf-ai/keyword-first-grammar-协议.md`、`tools/vscode/shelf-ai/examples/合取模块.sf`、`tools/vscode/shelf-ai/examples/父子任务状态记录模块.sf` |
| framework 变化默认只做 lint 自动化 | 默认开启 `shelf.frameworkLintOnlyOnFrameworkChanges`：framework 受控 Markdown 变化时，保存与 watcher 自动化停在 lint，不再隐式跑 change validate / materialize / mypy；手动 validate / preflight 保持完整链路 | `tools/vscode/shelf-ai/package.json`、`tools/vscode/shelf-ai/extension.js`、`tools/vscode/shelf-ai/README.md` |
| 框架树作者源投影与状态栏直达 | 框架树正式关系来源固定为 `framework/**` 作者源，但画布已收敛为模块级作者图：模块来自 `Lx-My-*.md`，`B*` 与规则参与基只保留在 hover / inspector 与模块关系提取里，不再作为可见节点或基组合边；若上层基定义引用下层模块（如 `Lx.My[...]`），画布折叠为模块箭头。状态栏点击支持直接动作与 `quickPick` 二选一分叉；保存 framework 文件会后台刷新已打开树图（不强制弹出/抢焦点）；若显式关闭 framework-lint-only 自动化，则会先跑保存触发校验/物化再刷新树图；若全量物化失败且命令基于 `materialize_project.py`，会启用 `--allow-framework-only-fallback` 先刷新 `canonical.framework` 并标记 degraded | `tools/vscode/shelf-ai/tree_runtime_models.js`、`tools/vscode/shelf-ai/extension.js`、`tools/vscode/shelf-ai/package.json`、`tools/vscode/shelf-ai/test_tree_runtime_models.js`、`src/project_runtime/compiler.py`、`scripts/materialize_project.py` |
| 零项目 bootstrap 兼容 | 当仓库暂时没有 `projects/*/project.toml` 时，插件把工作区视为合法 bootstrap/no-project 状态：preflight 不再报错，首页明确提示此时只建立 framework authoring 能力，evidence tree 与正式跨层导航保持未建立 | `tools/vscode/shelf-ai/extension.js`、`tools/vscode/shelf-ai/guarding.js`、`tools/vscode/shelf-ai/test_guarding.js`、`tools/vscode/shelf-ai/README.md` |
| 不提供受控任务会话 | 插件不再维护“需求 -> framework 映射授权”会话状态；保存阶段与工作台状态区不再依赖会话语义 | `tools/vscode/shelf-ai/extension.js`、`tools/vscode/shelf-ai/package.json`、`tools/vscode/shelf-ai/README.md` |
| 问题级别区分（error/warning） | 插件状态栏与 diagnostics 已按 `warning/error` 分级显示，不再统一按 error 呈现 | `tools/vscode/shelf-ai/extension.js`、`tools/vscode/shelf-ai/correspondence_runtime.js` |
| 契约与现状分离 | 契约文档不再写“当前已落地”结论，现状单独落在本文件 | `tools/vscode/shelf-ai/插件设计与实现契约.md`、本文件 |

## 3. 全模块静态对象落地现状（对应契约第 13.8 节）

| 契约条目 | 当前现状 | 证据 |
| --- | --- | --- |
| `framework/**` 全模块静态落地 | 静态注册表已支持扫描 `src/project_runtime/static_modules/**/*.py`，因此既能识别 `modules/**` 下的逐模块文件，也能识别 `chunk_tagging_contracts.py` 这类根级聚合 contract bundle；但截至当前快照，静态 registry 仍未完全覆盖仓库里的全部 framework module | `src/project_runtime/static_modules/modules/**`、`src/project_runtime/static_modules/chunk_tagging_contracts.py`、`src/project_runtime/static_modules/registry.py`、`tests/test_four_layer_canonical.py` |
| Code 层仅走静态注册表 | `code_layer` 通过 `get_static_module_contract_bundle` 装配模块合约类型 | `src/project_runtime/code_layer.py` |
| 不保留 pilot 豁免 | `codegen_consistency_guard` 对全部模块执行 effective extraction | `src/project_runtime/codegen_consistency_guard.py` |
| 防回退测试 | 测试中断言 `framework module 集合 == STATIC_MODULE_CONTRACTS 集合` | `tests/test_four_layer_canonical.py` |

## 4. 最近一次验证快照

- `npm --prefix tools/vscode/shelf-ai run check:webview-types`：通过
- `npm --prefix tools/vscode/shelf-ai test`：通过
- `uv run mypy`：通过
- `uv run pytest -q tests/test_no_hardcode_guard.py`：通过
- `uv run pytest -q`：失败（当前剩余 4 项，涉及静态 contract 全覆盖断言、framework 依赖投影期望与已移除的 `message_queue` 索引路径；非本次 `shelf-ai` 发版门禁）
- `uv run python scripts/materialize_project.py`：通过
- `uv run python scripts/validate_canonical.py`：通过
- `uv run python scripts/validate_canonical.py --check-changes`：通过

> 说明：本节是“最近一次可复验结果”快照，不替代契约，不替代 CI 历史。
