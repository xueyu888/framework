# knowledge_base_basic 框架严格映射基线

## 1. 目的

本文件把以下两份框架标准落实到 `knowledge_base_basic` 的当前实现：

- [前端框架标准模块](../../framework/frontend/L2-M0-前端框架标准模块.md)
- [知识库工作台场景模块](../../framework/knowledge_base/L2-M0-知识库工作台场景模块.md)

目标不是口头对应，而是把当前产品从框架到代码的收敛链写清楚：

1. `framework -> product spec`
2. `framework -> implementation config`
3. `framework -> runtime/code`
4. `framework -> validation`

当前实现的主链已经调整为：

`framework markdown -> framework IR -> product_spec.toml -> implementation_config.toml -> contracts/runtime bundle -> rule validation -> generated evidence`

关键落点：
- IR 解析：[src/framework_ir/](../../src/framework_ir/)
- 产品编译：[knowledge_base.py](../../src/project_runtime/knowledge_base.py)
- 前端通用契约与规则：[src/frontend_kernel/](../../src/frontend_kernel/)
- 知识库工作台契约与规则：[src/knowledge_base_framework/](../../src/knowledge_base_framework/)
- Product Spec：[product_spec.toml](./product_spec.toml)
- Implementation Config：[implementation_config.toml](./implementation_config.toml)
- 生成产物：`generated/framework_ir.json`、`generated/product_spec.json`、`generated/implementation_bundle.py`、`generated/generation_manifest.json`

状态约定：
- `已落地`：当前实现已有稳定代码和验证落点
- `部分落地`：当前已落代码，但仍有可继续抽象或扩展的空间
- `缺口`：当前没有稳定代码或验证落点

## 2. framework -> product spec

### 2.1 frontend 边界 -> `product_spec.toml`

约定：
- 框架边界保持大类建模
- `product_spec.toml` 以同名大类 `section` 承载产品值
- section 内字段为该边界下的小类变化项
- 跨边界联动不通过字段归属解决，而通过 `R*` 校验解决

| 边界 | 当前配置落点 | 状态 | 说明 |
|---|---|---|---|
| `SURFACE` | `[surface]`、`[surface.copy]` | 已落地 | shell、layout、preview_mode、density 与 surface 文案都归 `surface` 大类 |
| `VISUAL` | `[visual]` | 已落地 | 品牌、强调色、表面预设、圆角级和字体级都归 `visual` 大类 |
| `ROUTE` | `[route].home/workbench/knowledge_list/knowledge_detail/document_detail_prefix/api_prefix` | 已落地 | 聊天页、知识库页、文档详情页和 API 前缀都来自产品规格 |
| `A11Y` | `[a11y].reading_order/keyboard_nav/announcements` | 已落地 | 阅读顺序、键盘路径与播报策略在产品层固定 |
| `LIBRARY` | `[library]`、`[library.copy]` | 已落地 | 知识库身份、列表策略、写入允许性与搜索文案都归 `library` 大类 |
| `PREVIEW` | `[preview]` | 已落地 | 锚点模式、TOC 与引用抽屉变体在产品层固定 |
| `CHAT` | `[chat]`、`[chat.copy]` | 已落地 | 对话模式、引用样式、欢迎文案与输入文案都归 `chat` 大类 |
| `CONTEXT` | `[context]` | 已落地 | 当前文件/章节与引用阈值都显式配置 |
| `RETURN` | `[return]` | 已落地 | 返回目标、锚点恢复与 citation card 变体显式配置 |

### 2.2 framework -> implementation config

| 归属 | 当前配置落点 | 状态 | 说明 |
|---|---|---|---|
| `frontend implementation` | `[frontend].renderer/style_profile/script_profile` | 已落地 | 前端实现 profile 只描述如何做，不改产品真相 |
| `backend implementation` | `[backend].renderer/transport/retrieval_strategy` | 已落地 | 后端实现 profile 约束传输协议与检索策略 |
| `evidence` | `[evidence].product_spec_endpoint` | 已落地 | 编译后的产品规格通过独立证据接口暴露 |
| `artifact naming` | `[artifacts].*` | 已落地 | 生成产物文件名归实现配置，而不归产品层 |

## 3. framework -> code

### 3.1 frontend `L2-M0`

| 框架项 | 当前代码落点 | 状态 | 说明 |
|---|---|---|---|
| `C1-C3` | [contracts.py](../../src/frontend_kernel/contracts.py)、[frontend.py](../../src/knowledge_base_runtime/frontend.py) | 已落地 | 已编译出显式前端 contract、页面壳体与交互组件结构 |
| `B1-B2` | [frontend.py](../../src/knowledge_base_runtime/frontend.py)、[frontend_script.py](../../src/knowledge_base_runtime/frontend_script.py) | 已落地 | 会话侧栏、聊天主区、引用抽屉和辅助页面都接到同一运行时 bundle |
| `R1-R4` | [validators.py](../../src/frontend_kernel/validators.py) | 已落地 | 前端规则已经成为启动期硬校验 |

### 3.2 knowledge_base `L2-M0`

| 框架项 | 当前代码落点 | 状态 | 说明 |
|---|---|---|---|
| `C1-C3` | [workbench.py](../../src/knowledge_base_framework/workbench.py)、[backend.py](../../src/knowledge_base_runtime/backend.py) | 已落地 | 工作台主链、上下文回路与引用返回路径都已显式化 |
| `B1-B3` | [backend.py](../../src/knowledge_base_runtime/backend.py)、[app.py](../../src/knowledge_base_runtime/app.py) | 已落地 | 页面、API、产品规格接口与返回路径围绕同一 chat-first 闭环输出 |
| `R1-R4` | [validators.py](../../src/knowledge_base_framework/validators.py) | 已落地 | 4 条规则均已成为启动期可执行校验 |

## 4. framework -> validation

| 规则域 | 校验入口 | 状态 | 说明 |
|---|---|---|---|
| frontend | [validators.py](../../src/frontend_kernel/validators.py) | 已落地 | 检查承载面、交互动作、扩展插槽和禁止组合 |
| knowledge_base | [validators.py](../../src/knowledge_base_framework/validators.py) | 已落地 | 检查工作台主链、引用回路、返回路径与禁止组合 |
| strict mapping | [validate_strict_mapping.py](../../scripts/validate_strict_mapping.py) | 已落地 | 检查 `framework/*.md + product_spec.toml + implementation_config.toml -> generated/*` 是否严格一致 |

## 5. 编译产物映射

| 产物 | 来源 | 用途 |
|---|---|---|
| `generated/framework_ir.json` | `framework markdown -> framework IR` | 冻结被选中的 primary modules 与闭包模块 |
| `generated/product_spec.json` | `framework + product_spec.toml -> compiled product spec` | 输出语言无关的产品真相 |
| `generated/implementation_bundle.py` | `product spec + implementation config + contracts/runtime bundle` | 生成后的实现侧导入入口 |
| `generated/generation_manifest.json` | `framework + product spec + implementation config + generator output hashes` | 证明当前产物只能由源输入重新生成 |

## 6. 结论

`knowledge_base_basic` 当前已经具备以下严格映射能力：

- 框架文档被解析为可追溯 IR
- `product_spec.toml` 只负责产品真相，`implementation_config.toml` 只负责技术细化
- `projects/<project_id>/generated/*` 只允许由生成器物化，不允许手改
- 前端通用层和知识库工作台层都产出显式 contract
- 主界面保持 ChatGPT 风格：会话侧栏 + 聊天主区 + 按需来源抽屉
- `R1-R4` 在启动期变成硬校验
- 编译产物会把 IR、产品真相和实现 bundle 分开写出
