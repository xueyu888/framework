整体思路：
首先，该模块接收一个文本单元输入，接收一个标签定义输入。
在模块中将文本单元和标签定义中需要的内容（除标识符之外的内容），加上提示词，拼接成为一个单次评分输入单元。
将这个单次评分单位通过大模型接口提交给大模型进行评分。
将得到的评分结果进行结构化为匹配评分结果和匹配评分原因。
将结果、原因、加上文本标识、标签标识后作为单次匹配评分记录输出。

所以这个模块的基包括：文本单元、标签定义、单次评分输入单元，大模型接口，单次匹配评分记录。
其中单次评分输入单元包括文本内容，标签语义，提示词。
单次匹配评分记录包括文本标识，标签标识，评分，原因。