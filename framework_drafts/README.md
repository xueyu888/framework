# Framework Drafts

这里是框架文档草稿区。

- `framework_drafts/` 下的 Markdown 仍可使用 Shelf AI 的 `@framework` 模板、补全、引用跳转与 hover。
- 草稿默认不进入正式 `framework/` 树，不参与项目物化，也不会触发严格映射与代码一致性要求。
- 当草稿成熟后，通过 Shelf AI 的 `Publish Current Framework Draft` 命令发布到正式 `framework/` 目录。
- 发布后，文档才进入正式框架树、治理树和物化链。
