# 模块名:ModuleName

@framework

## Goal

goal <GoalName> := <本模块的目标是什么，作为本框架的故事开始>

## Base

base <BaseName> := <一句定义或 Lx.My::Base.Name>
base <BaseName> := ...
base <BaseName> := ...

## Combination Principles

form <PrincipleName> on <base.Name / cs.Name / form> := <什么能组成什么>
sat <PrincipleName> on <base.Name / cs.Name / form> := <什么条件下成立>
id <PrincipleName> on <base.Name / cs.Name / form> := <什么算同一个组合>
norm <PrincipleName> on <base.Name / cs.Name / form> := <同一个组合的标准写
form <PrincipleName> on <...> := ...
sat <PrincipleName> on <...> := ...
id <PrincipleName> on <...> := ...
norm <PrincipleName> on <...> := ...

## Combination Space

cs.<CombinationName> := <base.Name / cs.Name / ...> by <cp.form.Name, cp.sat.Name, cp.id.Name, cp.norm.Name>
cs.<CombinationName> := ...

## Boundary

### Input

in <InputName> := payload(<...>), cardinality(<...>), to(<base.Name / cs.Name>)
in <InputName> := ...

### Output

out <OutputName> := payload(<...>), cardinality(<...>), from(<cs.Name>)
out <OutputName> := ...

### Parameter

param <ParameterName> := domain(<...>), affects(<cp.form.Name / cp.sat.Name / cp.id.Name / cp.norm.Name / cs.Name / bd.in.Name / bd.out.Name>)
param <ParameterName> := ...
